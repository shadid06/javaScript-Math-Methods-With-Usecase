// Math.sign() – Check if a number is positive or negative

// Math.sign() method returns the sign of a number, indicating whether the number is positive, negative or zero.

// If the number is positive, Math.sign() returns 1.
// If the number is negative, Math.sign() returns -1.
// If the number is zero, Math.sign() returns 0.

console.log(Math.sign(5)); // 1
console.log(Math.sign(-5)); // -1
console.log(Math.sign(0)); // 0

// Project Use Case: Detect upvote/downvote trends.
