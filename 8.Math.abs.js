// Math.abs() – Make a number always positive

let negativeNumber=-10;
let positiveNumber=Math.abs(negativeNumber);
console.log(positiveNumber); // 10

// Project Use Case: Calculate difference between two scores or positions.